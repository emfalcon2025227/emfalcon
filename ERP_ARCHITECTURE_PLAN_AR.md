# المخطط المعماري وخطة دمج قواعد البيانات لتطوير نظام ERP عقاري متكامل
**Senior Software Architecture, Database Design & ERP Solution Integration Plan**
*تاريخ إعداد الدراسة والمخطط: 18 أغسطس 2026*

---

## A. Architecture Principles (المبادئ المعمارية الحاكمة)
لتحويل نظام **صقر الإمارات للعقارات** الحالي من برنامج إدارة عقود وشيكات وصيانة صلب إلى نظام تخطيط موارد المؤسسات **(ERP)** عقاري ومالي شامل، قمنا بوضع المبادئ المعمارية التالية لتوجيه كافة أعمال التطوير المستقبلية لضمان الاستقرار المالي وتجنب تعارض البيانات:

1. **قاعدة إعادة الاستخدام أولاً (Reuse First, Extend Second, Create New Last)**:
   نعتبر النظام الحالي هو النواة الصلبة والمركزية للمشروع (**Core System**). يُمنع منعاً باتاً استنساخ أو استبدال الجداول الأساسية المعتمدة (الملاك، العقارات، الوحدات، المستأجرين، العقود، الشيكات المرتجعة، القضايا، الأرشيف، الصيانة). بدلاً من ذلك، نُعيد استخدامها كما هي، ثم نُوسعها تالياً بالحقول والوظائف المطلوبة، ولا نُنشئ كيانات جديدة إلا إذا كانت مخصصة لمهام جديدة كلياً (مثل شجرة الحسابات، الدليل المحاسبي، الفواتير، عمولات الإيجار، وتفتيش الوحدات).
2. **التوافقية الرجعية الكاملة (Strict Backward Compatibility)**:
   يجب أن يستمر الكود الحالي وقواعد البيانات النشطة سحابياً ومحلياً بالعمل بكفاءة 100% أثناء وبعد دمج الكيانات والموديولات الجديدة. يُمنع إجراء أي تغييرات برمجية تكسر البيانات الحالية أو تؤدي إلى تصفير الذاكرة المحلية للمستخدمين.
3. **مصدر الحقيقة المالي الأوحد (Single Source of Financial Truth)**:
   تُقيد جميع القيم والأرصدة المالية بشكل أحادي ومحكم لمنع وجود أكثر من مصدر مستقل لحساب نفس القيمة المالية (مثلاً: رصيد المستأجر المعلق أو مبالغ التحصيل يجب أن تُقاد برمجياً من حركات القيود اليومية وسندات القبض المعتمدة فقط، ولا يتم حسابها بشكل عشوائي في شاشات عرض متعددة).
4. **التتبع والأثر المحاسبي الإجباري (Every Action is Traceable)**:
   كل حدث تشغيلي ذو أثر مالي (مثل: إبرام عقد، ارتداد شيك، دفع صيانة، إثبات عمولة، تسجيل مصروف تشغيلي) يجب أن يولد تلقائياً قيداً محاسبياً متوازناً (قيد يومية مدين ودائن) لضمان تتبع التدفقات المالية بدقة وبما يتوافق مع المعايير المحاسبية الدولية والمحلية لخدمة التدقيق والرقابة المالية.

---

## B. Current System → Future ERP Mapping (خريطة التحول للـ ERP)

بناءً على تقرير التدقيق الفعلي المعزز `AUDIT_REPORT_AR.md` والتحليل المعماري، نحدد هيكلة ترحيل وتطوير الكيانات الحالية والمستقبلية كالتالي:

| اسم الكيان (Entity) | المجموعة الحالية في Firestore | الغرض التشغيلي الحالي | العلاقات الحالية الرئيسية | الحالة الحالية في التقرير | الإجراء المعماري المستقبلي (Future Action) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **الملاك (Owners)** | `owners` | إدارة حسابات وملفات ملاك العقارات البنكية والشخصية | لا توجد علاقات خارجية مباشرة واردة | `IMPLEMENTED` | **إعادة استخدام وإضافة روابط**: ربط المالك مباشرة بكشف الحساب الشامل والعمولات والمقبوضات والفواتير. |
| **العقارات (Properties)** | `properties` | تسجيل المباني والفلل السكنية والتجارية وموقعها | `ownerId` -> `owners` | `IMPLEMENTED` | **إعادة استخدام وتوسيع**: ربط العقار بجدول المصروفات التشغيلية لحساب ربحية المبنى وموازنته. |
| **الوحدات (Units)** | `units` | تتبع تفاصيل الشقق والمكاتب وحالاتها الإيجارية | `propertyId` -> `properties`, `currentTenantId` -> `tenants` | `IMPLEMENTED` | **توسيع**: ربط الوحدة بـ Unit Inspections (حركات الفحص) وتاريخ الصيانة المفصل. |
| **المستأجرين (Tenants)** | `tenants` | تتبع بيانات المستأجرين وحساب مؤشرات تقييم المخاطر | لا توجد علاقات خارجية مباشرة | `IMPLEMENTED` | **توسيع**: ربط حساب المستأجر بـ Tenant Ledger (دفتر حساب المستأجر) والفواتير الصادرة. |
| **العقود (Leases)** | `leases` | تسجيل العقود وتاريخها والتحقق من حالة العقد | روابط مباشرة بالمستأجر، المالك، العقار، والوحدة | `IMPLEMENTED` | **توسيع**: ربط العقد بمحرك العمولات التلقائي ومقاصة العقد والفواتير الدورية للأقساط. |
| **الشيكات (Cheques)** | `cheques` | تتبع أوراق الشيكات المستلمة وقيمها ومواعيد استحقاقها | مرتبطة بالمستأجر، المالك، العقار، الوحدة، والعقد | `IMPLEMENTED` | **إعادة استخدام**: ربط الشيكات بـ Journal Entries (القيود اليومية) فور التحصيل أو الارتداد لتسجيل الأثر المالي آلياً. |
| **التحصيلات (Collections)** | `collections` | تسجيل المبالغ المقبوضة وسندات القبض المالي | `chequeId`, `tenantId`, `ownerId` | `IMPLEMENTED` | **توسيع**: ربط حركات القبض بدليل الحسابات (شجرة الحسابات) لإنشاء قيد مالي مزدوج آلي لكل سند قبض. |
| **القضايا (Cases)** | `cases` | إدارة النزاعات القانونية وجدول الجلسات والتسويات | روابط بـ `tenantId`, `ownerId`, `linkedChequeIds` | `IMPLEMENTED` | **إعادة استخدام**: الإبقاء على موديول القضايا مع ترحيل مخرجات التسويات وجداول أقساط التسوية كقيود محاسبية ومستندات قبض. |
| **الصيانة (Maintenance)** | `maintenance_requests` | معالجة طلبات الصيانة وتوزيع الفواتير والمسؤول عن الدفع | روابط بـ `propertyId`, `unitId`, `assignedTechnicianId` | `IMPLEMENTED` | **توسيع**: ربط الفواتير الضريبية للصيانة بموديول الفواتير الموحد والمصروفات، وربط التكلفة بدفتر أستاذ المالك والمستأجر. |
| **الأرشيف (Archive)** | `archive` | توفير مستودع ملفات رقمي قابل للبحث والتصنيف | `recordId` كمعرف ديناميكي للمستند المرتبط | `IMPLEMENTED` | **إعادة استخدام وتطوير**: ربط مستندات ومخرجات موديولات الـ ERP الجديدة كالفواتير، سندات القيد، تقارير الفحص، كشوف الحساب آلياً بالأرشيف. |
| **الإشعارات (Notifications)** | `notifications` | تتبع إرسال المراسلات والتحقق من حالتها وقنواتها | `tenantId`, `chequeId`, `caseId` | `PARTIALLY IMPLEMENTED` (Simulated APIs) | **تطوير وبناء**: ربط أحداث استحقاق الفواتير، انتهاء العقد، الجلسات، وعمولة المكتب بقنوات الإرسال وتطوير التكامل الفعلي. |
| **العمليات المحذوفة (Historical)** | `historicalRecords` | ترحيل وحفظ البيانات الممحوة كلياً واسترجاعها | روابط بالمستخدم الذي قام بالعملية وتخزين الكائن | `IMPLEMENTED` | **إعادة استخدام وتوسيع**: حماية مدخلات القيود المحاسبية، الفواتير، ومحاضر الفحص الجديدة وتوفير إمكانية تراجع وحذف آمن لها. |

---

## C. Existing Entities (الكيانات الأساسية الحالية)
تضم هذه المجموعة كافة الكيانات التي تم جردها بدقة من الكود البرمجي الحالي (`src/types.ts`) ولقد تقرر الحفاظ عليها بالكامل وبنفس أسمائها وهيكلتها لمنع تشتت البيانات الحالية:

1. **Owners (الملاك)**: لتخزين هويات الملاك، أرقام الرخص وهويات الإمارات، وعناوين الحسابات المصرفية وأرقام الآيبان للتحويل والقبض.
2. **Properties (العقارات)**: لتحديد المباني السكنية والتجارية، المناطق، الطوابق، وعدد الوحدات المتاحة وحالة العقار النشطة.
3. **Units (الوحدات)**: لتفصيل الشقق، المكاتب، المحلات، القيمة السنوية المستهدفة للإيجار، وعدادات الكهرباء والماء وحالتها الآنية (شاغرة، مشغولة، صيانة).
4. **Tenants (المستأجرين)**: لتخزين بيانات الأفراد والشركات والتحقق من مستويات الأمان ومؤشرات الخطورة وتقييم المستأجر.
5. **Leases (عقود الإيجار)**: لتحديد التواريخ، الرسوم، الأقساط المجدولة، والتأمين، ومستويات كسر عقود الإيجار.
6. **Cheques (الشيكات)**: لإدارة حافظة الشيكات البنكية وحالاتها وحركات الارتداد والتحصيل وإصدار سندات بديلة.
7. **Collections (التحصيلات المالية)**: لتوثيق سندات القبض ومقدار المبالغ المستلمة من المسددين وطرق تحصيلها.
8. **Rental Cases (القضايا الإيجارية)**: لتتبع النزاعات ومحاضر الجلسات ومطالبات اللجان وفض المنازعات والتسويات المجدولة.
9. **Maintenance (طلبات الصيانة)**: لمعالجة بلاغات الصيانة وتوزيع الفنيين وتكاليف قطع الغيار والعمالة وإسناد التكلفة.
10. **Archive (الأرشيف)**: لتخزين وتصنيف ومزامنة المستندات الرسمية وعقود الإيجار وصور الهويات وصور الشيكات المرتجعة.
11. **Notifications (الإشعارات والرسائل)**: لمراقبة وتوثيق رسائل المتابعة وحالة المراسلات عبر قنوات الواتساب والبريد الموجهة للمتعاملين والمستأجرين.

---

## D. Extended Entities (الكيانات الحالية المطورة)
لكي ندعم الانتقال إلى ERP شامل دون أن نكسر قواعد البيانات الحالية، سنقوم **بتوسيع (Extend)** الكيانات الحالية في قاعدة البيانات بإضافة بعض الحقول المحورية الإضافية بشكل غير تدميري (Non-destructive Extensibility) كالتالي:

### 1. توسيع جدول عقود الإيجار (`leases`)
سنضيف الحقول التالية لدعم محرك العمولات المحتسب للطرفين وتتبع الفواتير:
* `ownerCommissionType`: نوع عمولة المالك (`PERCENTAGE` | `FIXED` | `NONE`).
* `ownerCommissionValue`: قيمة عمولة المالك المحتسبة (مبلغ أو نسبة).
* `tenantCommissionType`: نوع عمولة المستأجر (`PERCENTAGE` | `FIXED` | `NONE`).
* `tenantCommissionValue`: قيمة عمولة المستأجر المحتسبة.
* `isCommissionInvoiced`: هل تم إصدار فاتورة عمولة للشركة؟ (`boolean`).
* `isCommissionCollected`: هل تم استلام قيمة العمولات كلياً؟ (`boolean`).

### 2. توسيع جدول المستأجرين (`tenants`)
* `accountLedgerId`: معرف دفتر الحساب الفريد للمستأجر لربطه المباشر بالنظام المحاسبي وشجرة الحسابات.

### 3. توسيع جدول طلبات الصيانة (`maintenance_requests`)
* `invoiceId`: رابط مباشر بمعرف الفاتورة الضريبية الموحدة الصادرة في موديول الفواتير الجديد.
* `operatingExpenseId`: في حال تحويل طلب الصيانة إلى مصروف تشغيلي عام على العقار، يربطه هذا الحقل بجدول المصاريف التشغيلية الجديد.

---

## E. New ERP Entities (الكيانات الجديدة كلياً لـ ERP)

تعتبر هذه الكيانات بمثابة البنية التحتية الأساسية التي سنقوم بإنشائها وتصميمها برمجياً لربط كافة موديولات النظام وتحقيق مفهوم ERP الشامل والمتكامل:

### 1. جدول الحسابات ودليل الأستاذ العام (`chart_of_accounts`)
* **الغرض**: الدليل والقاموس المالي لتعريف شجرة الحسابات (أصول، خصوم، حقوق ملكية، إيرادات، مصروفات).
* **الحقول الرئيسية**:
  * `id` (Primary Key - UUID)
  * `accountCode` (كود الحساب الفريد، مثل: `1101` للنقدية، `4101` لإيراد الإيجارات).
  * `accountNameAr`, `accountNameEn` (اسم الحساب باللغتين، مثل: "حساب الصندوق والأموال السائلة"، "إيرادات التأجير").
  * `parentAccountId` (رابط للحساب الأب في حال البنية الهرمية - اختياري).
  * `accountType` (النوع: `ASSET`, `LIABILITY`, `EQUITY`, `REVENUE`, `EXPENSE`).
  * `balance` (الرصيد المالي الحالي للحساب).
  * `isSystemAccount` (هل هو حساب نظام رئيسي يمنع حذفه يدوياً - `boolean`).
  * `createdAt` (تاريخ الإنشاء).

### 2. جدول الفواتير الموحد (`invoices`)
* **الغرض**: إصدار وتتبع الفواتير الضريبية والمالية الرسمية لكافة العمليات (الإيجارات، الصيانة، العمولات، الخدمات).
* **الحقول الرئيسية**:
  * `id` (Primary Key)
  * `invoiceNumber` (رقم الفاتورة المتسلسل الفريد)
  * `leaseId` (معرف العقد المرتبط - اختياري)
  * `tenantId` (معرف المستأجر الموجهة له الفاتورة - اختياري)
  * `ownerId` (معرف المالك المستفيد أو المطالب بالفاتورة - اختياري)
  * `invoiceType` (نوع الفاتورة: `RENTAL_INSTALLMENT`, `AGENCY_COMMISSION`, `MAINTENANCE_CHARGE`, `LATE_FEE`, `UTILITY_BILL`, `OTHER`)
  * `invoiceDate` (تاريخ الفاتورة)
  * `dueDate` (تاريخ الاستحقاق المالي)
  * `subtotal` (المجموع الفرعي قبل الضريبة)
  * `taxAmount` (قيمة ضريبة القيمة المضافة VAT - مثلاً 5%)
  * `discountAmount` (قيمة الخصومات الممنوحة)
  * `totalAmount` (المجموع الكلي النهائي للفاتورة)
  * `paidAmount` (المبلغ المسدد والمدفوع فعلياً من هذه الفاتورة)
  * `outstandingAmount` (المبلغ المتبقي المعلق غير المسدد)
  * `status` (الحالة: `UNPAID`, `PARTIALLY_PAID`, `FULLY_PAID`, `VOIDED`, `OVERDUE`)
  * `notes` (ملاحظات الفاتورة).

### 3. جدول دفتر العمولات وعمولات العقود (`commissions_ledger`)
* **الغرض**: تسجيل ومطالبة وتحصيل العمولات المستحقة للشركة من الملاك والمستأجرين.
* **الحقول الرئيسية**:
  * `id` (Primary Key)
  * `leaseId` (معرف العقد المرتبط - Foreign Key)
  * `partyType` (الطرف المستحق عليه العمولة: `OWNER` | `TENANT` | `BOTH`)
  * `ownerCommissionAmount` (قيمة عمولة المالك المستحقة)
  * `tenantCommissionAmount` (قيمة عمولة المستأجر المستحقة)
  * `totalCommissionAmount` (إجمالي إيراد العمولة المتوقع للمكتب)
  * `ownerInvoiceId`, `tenantInvoiceId` (روابط بالفواتير المصدرة للطرفين للتحصيل)
  * `dueDate` (تاريخ استحقاق العمولات)
  * `collectedAmount` (المبالغ التي تم تحصيلها فعلياً من العمولات)
  * `outstandingAmount` (الرصيد المتبقي المعلق غير المحصل)
  * `status` (الحالة: `PENDING`, `PARTIALLY_COLLECTED`, `FULLY_COLLECTED`, `FAILED_TO_COLLECT`)
  * `createdAt`

### 4. جدول المصروفات التشغيلية للعقارات (`property_operating_expenses`)
* **الغرض**: تتبع كافة النفقات المالية والتشغيلية المباشرة وغير المباشرة للمباني والعقارات بشكل مستقل عن أعطال الصيانة الفردية.
* **الحقول الرئيسية**:
  * `id` (Primary Key)
  * `expenseNumber` (رقم سند الصرف/المصروف)
  * `propertyId` (معرف العقار المرتبط - Foreign Key)
  * `unitId` (معرف الوحدة في حال كان المصروف مخصصاً لوحدة معينة - اختياري)
  * `category` (التصنيف: `MUNICIPALITY_FEES` "رسوم البلدية"، `INSURANCE` "التأمين السنوي"، `CLEANING` "أعمال النظافة"، `SECURITY` "الحراسة والأمن"، `UTILITIES` "الكهرباء والماء المشتركة"، `GENERAL_REPAIRS` "الترميمات العامة للمبنى")
  * `costBearer` (من يتحمل التكلفة: `OWNER` المالك، `TENANT` المستأجر، `MANAGEMENT` مكتب الإدارة، `SHARED` مشترك)
  * `amount` (مبلغ المصروف قبل الضريبة)
  * `taxAmount` (قيمة الضريبة إن وجدت)
  * `totalAmount` (إجمالي مبلغ المصروف النهائي)
  * `paymentDate` (تاريخ دفع المصروف)
  * `paymentMethod` (طريقة السداد)
  * `vendorName` (اسم المورد/المقاول أو الجهة الحكومية)
  * `journalEntryId` (معرف القيد اليومي المحاسبي المولد للمصروف)
  * `createdAt`

### 5. جدول محاضر الفحص ومعاينة الوحدات (`unit_inspections`)
* **الغرض**: تتبع حالة الوحدات السكنية بشكل بصري ووصفي دقيق عند تسليمها للمستأجر (Check-in) أو إخلائها واستلامها منه (Check-out) لحفظ الحقوق وتقدير الخصومات المالية من مبالغ التأمين.
* **الحقول الرئيسية**:
  * `id` (Primary Key)
  * `inspectionType` (نوع الفحص: `MOVE_IN` تسليم، `MOVE_OUT` استلام، `ROUTINE` تفتيش دوري، `TAKEOVER` استلام المبنى)
  * `unitId` (معرف الوحدة الخاضعة للفحص - Foreign Key)
  * `tenantId` (معرف المستأجر المرتبط - Foreign Key)
  * `leaseId` (معرف العقد - Foreign Key)
  * `inspectorName` (اسم الفاحص/الموظف المسؤول)
  * `inspectionDate` (تاريخ الفحص)
  * `status` (الحالة: `PASSED` ناجح، `FAILED` راسب وبحاجة لإصلاحات، `PENDING_REPAIRS` معلق للصيانة)
  * `inspectionItems` (مصفوفة تفصيلية باللغتين تشمل الأقسام: الأبواب، الجدران، التكييف، السباكة، الإضاءة، وحالتها: ممتازة، مقبولة، متضررة)
  * `damageDescription` (وصف الأضرار والتلفيات المكتشفة باللغتين)
  * `estimatedRepairCost` (التكلفة التقديرية لإصلاح الأضرار المكتشفة)
  * `deductedFromDeposit` (المبلغ المقتطع الفعلي من مبلغ تأمين المستأجر)
  * `photos` (روابط صور وفيديوهات إثبات حالة الشقة قبل وبعد الإشغال)
  * `createdAt`

### 6. قيود اليومية المحاسبية المزدوجة (`journal_entries`) وخطوط القيد (`journal_entry_lines`)
* **الغرض**: تسجيل وترحيل المعاملات المالية إلى دفتر أستاذ الأستاذ المالي لضمان الموازنة الرياضية (Double-Entry Bookkeeping Ledger).
* **حقول قيد اليومية (`journal_entries`)**:
  * `id` (Primary Key)
  * `entryNumber` (رقم القيد المتسلسل الفريد)
  * `entryDate` (تاريخ تحرير القيد المالي)
  * `description` (شرح القيد باللغتين، مثل: "إثبات سداد القسط الأول للعقد رقم L-101")
  * `totalDebit`, `totalCredit` (إجمالي الجانب المدين والجانب الدائن - يجب أن يكونا متساويين لنجاح حفظ القيد المالي)
  * `sourceDocumentType` (نوع المستند المصدر: `COLLECTION_RECEIPT`, `MAINTENANCE_INVOICE`, `COMMISSION_INVOICE`, `PROPERTY_EXPENSE`)
  * `sourceDocumentId` (معرف المستند الفعلي المرتبط)
  * `createdAt`
* **حقول خطوط القيد الفرعية (`journal_entry_lines`)**:
  * `id` (Primary Key)
  * `journalEntryId` (معرف القيد الأب - Foreign Key)
  * `accountId` (معرف الحساب من شجرة دليل الحسابات - Foreign Key)
  * `debitAmount` (المبلغ المدين - يوضع صفر في حال الدائن)
  * `creditAmount` (المبلغ الدائن - يوضع صفر في حال المدين)
  * `partnerId` (رابط لمعرف المالك أو المستأجر المرتبط بالقيد للتحليل المالي والترصيد)
  * `notes` (ملاحظات تفصيلية للسطر).

---

## F. Complete Entity Relationship Map (خريطة العلاقات الشاملة للـ ERP)

```
                  ┌───────────────┐
                  │    OWNERS     │
                  └───────┬───────┘
                          │ (1:N)
                          ▼
                  ┌───────────────┐
                  │  PROPERTIES   │
                  └───────┬───────┘
                          │ (1:N)
                          ▼
   ┌─────────────┐ (1:N)  ┌───────────────┐
   │ Inspections │◄───────┤    UNITS      │
   └─────────────┘        └───────┬───────┘
                                  │ (1:N)
                                  ▼
   ┌─────────────┐ (1:1)  ┌───────────────┐ (1:N)  ┌───────────────┐
   │   TENANTS   │◄───────┤    LEASES     ├───────►│  Commissions  │
   └──────┬──────┘        └───────┬───────┘        └───────┬───────┘
          │                       │                        │
          │ (1:N)                 │ (1:N)                  │ (1:N)
          ▼                       ▼                        ▼
   ┌─────────────┐ (1:N)  ┌───────────────┐ (1:1)  ┌───────────────┐
   │   INVOICES  │◄───────┤    CHEQUES    ├───────►│ Rental Cases  │
   └──────┬──────┘        └───────┬───────┘        └───────┬───────┘
          │                       │ (1:1)                  │ (1:1)
          │ (1:1)                 ▼                        ▼
          │               ┌───────────────┐ (1:N)  ┌───────────────┐
          ├──────────────►│  Collections  ├───────►│  Settlements  │
          │               └───────┬───────┘        └───────────────┘
          ▼                       │
   ┌─────────────┐ (1:N)          │
   │Journal Lines│◄───────────────┘
   └──────┬──────┘
          │ (N:1)
          ▼
   ┌─────────────┐
   │Chart of Accts│
   └─────────────┘
```

---

## G. Commission Engine Architecture (بنية محرك العمولات التلقائي)

يُصمم محرك العمولات ليعمل بشكل مؤتمت بالكامل عند توثيق عقد الإيجار ليعطي المكتب مرونة تامة لتحديد مصادر دخله وعمولاته ومطابقتها وحساب الرصيد المتبقي:

### الخوارزمية الرياضية لحساب العمولات والمستحقات:
* **قيمة عمولة المالك (Owner Commission)**:
  $$\text{Owner Commission Amount} = \begin{cases} \text{Annual Rent} \times \text{Owner Commission \%} & \text{if Type is PERCENTAGE} \\ \text{Fixed Value} & \text{if Type is FIXED} \end{cases}$$
* **قيمة عمولة المستأجر (Tenant Commission)**:
  $$\text{Tenant Commission Amount} = \begin{cases} \text{Annual Rent} \times \text{Tenant Commission \%} & \text{if Type is PERCENTAGE} \\ \text{Fixed Value} & \text{if Type is FIXED} \end{cases}$$
* **إجمالي إيراد عمولات العقد (Total Office Revenue)**:
  $$\text{Total Office Revenue} = \text{Owner Commission Amount} + \text{Tenant Commission Amount}$$
* **حساب المتبقي المعلق من العمولات للتحصيل**:
  $$\text{Outstanding Commission} = \text{Total Commission Amount} - \text{Collected Commission Amount}$$

---

## H. Owner Ledger & Statement (كشف حساب الملاك والترصيد المالي)

لتوفير إدارة مالية صارمة للملاك وعرض صافي الدفعات المستحقة لهم، قمنا بتصميم معمارية **كشف حساب المالك (Owner Ledger)** و**بيان حساب المالك (Owner Statement)**:

* **الفرق المعماري الجوهري بينهما**:
  * **Owner Ledger (دفتر حساب المالك)**: هو جدول مالي تحليلي داخلي يُخزن في قاعدة البيانات ويسجل كل المعاملات والقيود اليومية الخاصة بالمالك (الإيرادات الإيجارية كدائن، ومصاريف الصيانة والعمولات الإدارية والخصومات كمدين). يعمل على احتساب رصيد المالك التراكمي في أي لحظة.
  * **Owner Statement (بيان حساب المالك)**: هو مستند تقريري خارجي (قابل للتصفية، الطباعة، والتصدير كـ PDF/Excel) يتم توليده من واقع بيانات الـ Ledger لتقديمه ككشف حساب رسمي للمالك يوضح تفاصيل حركات مبانيه ومتحصلاته وصافي أرباحه المطلوب تحويلها لحسابه البنكي.

### المعادلة الرياضية لاحتساب صافي رصيد المالك المستحق (Net Owner Payout):

$$\text{Net Owner Payout} = \text{Collected Rental Rents} - \text{Owner Commissions} - \text{Paid Maintenance Costs} - \text{Property Expenses} - \text{Discounts \& Deductions}$$

---

## I. Tenant Ledger Architecture (بنية كشف حساب المستأجر)

يُعد كشف حساب المستأجر بمثابة السجل المحاسبي المركزي لتتبع مديونيات المستأجر وحالات دفعاته والتزاماته المالية دون تعارض مع نظام الشيكات المرتجعة الفعال والنشط حالياً:

### المعادلة الرياضية لحساب رصيد المستأجر الإجمالي (Tenant Balance Equation):

$$\text{Outstanding Balance} = \text{Total Invoiced Amounts (Rent + Commission + Damages)} - \text{Total Collected Cash/Cleared Payments}$$

---

## K. Invoice Architecture (بنية نظام الفواتير المستقل)

يُصمم موديول الفواتير ليكون المولد الرسمي للمطالبات والضامن لقوانين تحصيل ضريبة القيمة المضافة ومستند الأرشيف الرئيسي للتحصيل:

### 1. أنواع الفواتير المدعومة في الـ ERP:
* **Rental Invoice (فاتورة قسط إيجاري)**: تُصدر تلقائياً للمستأجر بناءً على جدول الأقساط قبل تواريخ استحقاق الشيكات بـ 10 أيام كإنذار ومطالبة رسمية بالسداد.
* **Agency Commission Invoice (فاتورة عمولة وخدمات)**: تُصدر لإثبات وتحصيل عمولة الإيجار المستحقة للشركة من الملاك أو المستأجرين فور فتح ملف العقد.
* **Maintenance Charge Invoice (فاتورة مطالبة صيانة وتكاليف)**: تُصدر لتحميل المستأجر أو المالك قيمة الأضرار وتكلفة قطع الغيار والعمالة المستخدمة لإصلاح الأعطال.
* **Operating Expense Invoice (فاتورة مصروف تشغيلي)**: تُسجل لإثبات المبالغ المطالب بسدادها لجهات خارجية أو موردين وتدخل كفاتورة شراء (Vendor Bill) في حساب المالك أو الشركة.

---

## L. Property Operating Expense Architecture (إدارة نفقات ومصاريف العقارات)

لتحقيق أقصى درجات الدقة والتمييز المالي في حسابات العقار الإجمالية، نفصل برمجياً ومعمارياً وتصنيفياً بين نوعين رئيسيين من المصاريف والنفقات:

1. **مصاريف صيانة الوحدات (Unit Maintenance Expenses)**:
   هي مصاريف صيانة علاجية أو وقائية مخصصة لخدمة وحدة سكنية معينة وتصليح أعطالها الفردية (مثل: إصلاح تسريب مياه في مطبخ شقة، أو صيانة مكيف داخل غرف النوم). ويتحمل تكلفتها الطرف المحدد بموجب شروط عقد الإيجار.
2. **المصاريف التشغيلية للعقار (Property Operating Expenses - OpEx)**:
   هي نفقات عامة وشاملة ومستمرة تخص الحفاظ على الهيكل العام للمبنى أو العقار ككل وتسيير أعماله (مثل: رسوم النظافة السنوية للممرات المشتركة، راتب حارس المبنى، فواتير إنارة السلالم والمصاعد المشتركة، التأمين السنوي للمبنى، ورسوم وتصاريح بلديات الإمارات). وتُحمل هذه التكاليف آلياً على حساب المالك العام وتُخصم من صافي متحصلات إيجارات المبنى قبل صرف دفعاته.

---

## M. Unit Inspection Architecture (محاضر المعاينة وتفتيش العقارات)

تعمل وحدة الفحص كصمام الأمان للحفاظ على أصول ملاك العقارات ومنع النزاعات وتقييم خصومات التأمين بدقة هندسية موثقة بصرياً:

```
                  ┌──────────────────────────────────────────────┐
                  │          Inspection Trigger Activated        │
                  │        (Move-In / Move-Out / Routine)        │
                  └──────────────────────┬───────────────────────┘
                                         │
                                         ▼
                  ┌──────────────────────────────────────────────┐
                  │        Select Inspection Items Checklist      │
                  │   (Doors, Walls, AC, Plumbing, Lights, etc.) │
                  └──────────────────────┬───────────────────────┘
                                         │
                                         ▼
                  ┌──────────────────────────────────────────────┐
                  │          Inspector Records Unit Status       │
                  │   - Condition: Excellent / Fair / Damaged    │
                  │   - Captures & Uploads Photos/Videos         │
                  └──────────────────────┬───────────────────────┘
                                         │
                                         ▼
                  ┌──────────────────────────────────────────────┐
                  │    In Case of Damage: Estimate Repair Cost   │
                  │ 1. Flag Inspection as FAILED                 │
                  │ 2. Generate Automated Repair Order           │
                  │ 3. Allocate Deduction Amount from Deposit    │
                  └──────────────────────────────────────────────┘
```

---

## N. Archive Integration Plan (خطة دمج الأرشيف المطور)

سيرتبط الأرشيف الإلكتروني الحالي (`archive`) بجميع الكيانات المستحدثة في الـ ERP الجديد بشكل ديناميكي كامل وتلقائي دون الحاجة لإعادة كتابة الكود:

* **طريقة الربط والدمج**:
  نستغل حقل `recordId` كمعرف ديناميكي فريد وحقل `category` ليعمل كـ (Polymorphic Relationship) في قاعدة البيانات. بمجرد رفع مستند جديد، يتم تحديد الصلاحيات (`isPrivate`) والروابط التلقائية كالتالي:
  * **الفواتير الموحدة (`invoices`)**: ترفع صورة الفاتورة للجدول الفرعي في الأرشيف بـ `category: "PAYMENTS"`.
  * **إثبات المصاريف التشغيلية (`property_operating_expenses`)**: ترفع فواتير الموردين والبلدية بـ `category: "PROPERTIES"`.
  * **محاضر الفحص والمعاينة (`unit_inspections`)**: ترفع صور وتأكيدات ومحاضر التسليم بـ `category: "UNITS"`.
  * **سندات القيود المحاسبية (`journal_entries`)**: تُرفق مستندات ومسوغات القيود اليومية تحت تصنيف مالي مخصص لضمان السرية والحماية.

---

## O. Notification & Simulated vs. Real Architecture (إرسال الإشعارات)

لمعالجة التنبيه وتوسيع قنوات التواصل لتغطي أحداث الـ ERP المتطورة، نقوم بهيكلة وتصميم التنبيهات كالتالي:

### 1. الأحداث والزنادات المستحدثة للـ ERP (Trigger Events):
* **Rent Due (فاتورة الإيجار مستحقة)**: تُرسل قبل 10 أيام من موعد استحقاق الشيك لتذكير المستأجر بتوفير السيولة.
* **Rent Overdue (فواتير متأخرة الدفع)**: تُرسل فور تأخر سداد القسط أو الفاتورة عن تاريخ الاستحقاق بـ 24 ساعة.
* **Commission Invoiced (عمولة صادرة)**: تُرسل للمستأجر أو المالك لمطالبته بسداد قيمة أتعاب إدارة وتحرير العقد.
* **Inspection Scheduled (موعد فحص ومعاينة)**: تُرسل للمستأجر لتأكيد موعد زيارة الفاحص للشقة أو تسليم المفاتيح.
* **Lease Expiry Reminder (اقتراب انتهاء العقد)**: تُرسل للمستأجر والمالك قبل 90 يوماً من انتهاء العقد للاتفاق على شروط التجديد أو الإخلاء.

### 2. التخطيط للانتقال من المحاكاة الفردية إلى الإرسال الفعلي (Transition Plan):

```
┌────────────────────────────────────────────────────────────────────────────────┐
│                          CURRENT SIMULATION ARCHITECTURE                       │
│ - Notification record created in DB in status "PENDING" or "SENT".             │
│ - Prepares quick simulated Direct WhatsApp URLs for manual clicks.              │
└───────────────────────────────────────┬────────────────────────────────────────┘
                                        │
                                        ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│                           FUTURE REAL INTEGRATION PLAN                         │
│ - Integrates direct REST Web-APIs in Backend Server:                           │
│   * WhatsApp Channel: Twilio WhatsApp API / Meta Cloud API (Official Gateways)  │
│   * Email Channel: SendGrid Mailer API / NodeMailer server integration.        │
│   * SMS Channel: UAE Local Telco APIs (Etisalat / Du SMS gateways).            │
│ - Automatic background workers (Cron jobs) periodically polling outstanding   │
│   balances and dispatching messages silently without any manual click action.  │
└────────────────────────────────────────────────────────────────────────────────┘
```

---

## P. Offline Sync & Race-Condition Architecture (إدارة الحماية والتزامن المالي)

نظراً لحساسية العمليات المالية في نظام الـ ERP ومخاطر حدوث تضارب في البيانات أثناء استخدام وضع السقوط الآمن المحلي (LocalStorage fallback)، قمنا بتصميم معمارية **تزامن وحماية البيانات (Conflict-Free Sync Engine)**:

### 1. تحليل وتصنيف مخاطر وتعارض البيانات:
* **تكرار السجلات وتولد شيكات مزدوجة (Duplicate Records)**: يحدث في حال تسجيل شيك أو سند قبض محلياً برقم تسلسلي، وتكرار نفس الرقم من مستخدم آخر على جهاز آخر في نفس الوقت.
* **تحديثات متعارضة وضائعة (Conflicting & Lost Updates)**: في حال تعديل حالة مستأجر لـ BLACKLISTED محلياً بسبب ارتداد شيك، وتعديله كـ ACTIVE وتجديد عقده من موظف آخر في نفس اللحظة عبر السحاب.

### 2. المعمارية المصممة لحل التعارض والتزامن (Sync Conflict Resolution Rules):
لتفادي هذه المخاطر تماماً، سنطبق الحقول والبروتوكولات الوقائية التالية في هيكلة البيانات:
* **إصدار السجل التراكمي (`recordVersion`)**: يبدأ من `1` ويزداد تلقائياً بمقدار `+1` مع كل تعديل.
* **تاريخ التعديل الدقيق (`updatedAt`) ومعرف الموظف المعدل (`updatedBy`)**.
* **مؤشرات المزامنة وحالة التعارض**:
  * `syncStatus`: تتبع حالة البيانات (`SYNCHRONIZED`, `PENDING_SYNC`, `CONFLICT`).
  * `conflictReason`: تصف تفاصيل التعارض لتسهيل التدقيق والمراجعة.
* **قاعدة الغلبة والحسم المحاسبي (Symmetric Sync Protocols)**:
  * **العمليات المالية والحذف**: العمليات المالية (مثل المقبوضات والفواتير وسندات الصرف) يُمنع فيها برمجياً تطبيق "الأخير هو الفائز" (Last-Write Wins) لمنع تشتت الحسابات. في حال وجود أي تضارب مالي، يتم تجميد السجل ووضعه في حالة `CONFLICT` مع إشعار الـ SUPER_ADMIN لمراجعة ومطابقة الفاتورة يدوياً.
  * **البيانات الأساسية وغير المالية**: يُطبق بروتوكول "الأحدث زمنياً هو الغالب" بناءً على مقارنة دقيقة للحقلين `updatedAt` و `recordVersion` بين السجل المحلي والسجل السحابي وتحديث الفارق تلقائياً.

---

## Q. Data Integrity Rules (قواعد وقيود سلامة وتكامل البيانات)

لتأمين النظام ضد الأخطاء الرياضية والمالية ومنع الخروقات التشغيلية، نصمم القواعد والتحققات الإجبارية التالية على نطاق قواعد البيانات:

1. **منع تكرار الشيكات وعقود الإيجار (Uniqueness Enforcement)**:
   * يجب أن يكون كود المالك، كود المستأجر، رقم العقد، ورقم الشيك قيماً فريدة بالكامل على مستوى النظام (`unique constraint`). يُمنع إدخال شيكين بنفس الرقم لنفس البنك والمستأجر.
2. **التحقق من صحة وصلاحية العلاقة الإيجارية (Referential Integrity Check)**:
   * لا يمكن إبرام عقد إيجار لوحدة إلا إذا كانت حالة الوحدة الحالية هي `VACANT`.
   * يمنع ربط عقد الإيجار بوحدة لا تنتمي للعقار المحدد في العقد أو بعقار لا يمتلكه المالك المسجل بالعقد.
3. **التحقق والتحكم في مبالغ السداد والقبض (Overpayment Prevention)**:
   * يُمنع تسجيل أي دفعة مالية بقيمة تفوق الرصيد المعلق والمتبقي المطلوب سداده للفاتورة أو الشيك المرتجع إلا بوجود تأكيد خطي مسبق وإدخال تسوية مالية واضحة للرصيد الفائض وتوجيهه كدفعة مقدمة (Prepayment Holding Account).
4. **حظر الأرصدة والقيم السالبة (No Negative Balance Rule)**:
   * يُمنع تماماً أن تصبح القيمة المالية للحقل المعلق (`outstanding`) أو رصيد الحساب البنكي أو رصيد الصندوق قيماً سالبة تحت أي ظرف من الظروف لضمان دقة العمليات المحاسبية.

---

## R. Unified Status Matrix (مصفوفة الحالات الموحدة للـ ERP)

```
┌─────────────────┐             ┌─────────────────┐             ┌─────────────────┐
│  Lease Status   ├────────────►│  Cheque Status  ├────────────►│ Invoice Status  │
├─────────────────┤             ├─────────────────┤             ├─────────────────┤
│ ACTIVE          │             │ PENDING         │             │ UNPAID          │
│ EXPIRED         │             │ CLEARED         │             │ PARTIALLY_PAID  │
│ TERMINATED      │             │ BOUNCED         │             │ FULLY_PAID      │
│ RENEWED         │             │ COLLECTED       │             │ VOIDED          │
│ UNDER_RENEWAL   │             │ REPLACED        │             │ OVERDUE         │
└─────────────────┘             └─────────────────┘             └─────────────────┘
                                         │
                                         ▼
                                ┌─────────────────┐
                                │   Case Status   │
                                ├─────────────────┤
                                │ NEW ➔ IN_PRG    │
                                │ SETTLEMENT ➔ CLS│
                                └─────────────────┘
```

### الترابط البرمجي وتطابق الحالات (Status Logic):
* إذا تم إبرام عقد إيجار (`leases` -> `ACTIVE`): تتغير حالة الفاتورة الأولى إلى `UNPAID` وتولد حركات الشيكات التابعة كـ `PENDING`.
* إذا ارتد شيك بنكي (`cheques` -> `BOUNCED`): تتغير حالة الفاتورة الإيجارية التابعة له تلقائياً إلى `OVERDUE` وتتحول حالة تحصيل الشيك المرتجع إلى `NOT_COLLECTED`.
* إذا تم تحصيل وسداد المبالغ بالكامل لاحقاً عبر المقبوضات (`collections` -> `fully applied`): تتغير حالة الشيك المرتجع تلقائياً إلى `COLLECTED` والحالة السابقة للتحصيل تصبح `FULLY_COLLECTED_AFTER_BOUNCE` وتتحول الفاتورة المرتبطة فوراً إلى `FULLY_PAID`.

---

## S. Financial Source of Truth (تحديد مصادر الحقائق المالية والأرصدة)

لمنع وجود أكثر من قيمة مالية مستقلة لنفس الرصيد المالي عبر واجهات التطبيق، نحدد ونقنن بدقة **مصدر الحقيقة المالي الأوحد** لكل قسم ونشاط:

1. **قيمة التعاقد الإجمالية (Contract Value)**: مصدر الحقيقة المطلق لها هو حقل `annualRent` داخل جدول عقود الإيجار (`leases`) الموثق رسمياً.
2. **جدول الأقساط والاستحقاق (Payment Schedule)**: مصدر الحقيقة هو مصفوفة `installments` الفرعية المسجلة داخل عقد الإيجار والتي تولد فواتير المطالبة لاحقاً.
3. **الدفعات والمقبوضات الفعلية (Actual Payments)**: مصدر الحقيقة المطلق والوحيد لها هو مجموع قيم حقول `amountEntered` داخل جدول المقبوضات وسندات القبض (`collections`) المقيدة والمطابقة دفترياً.
4. **رصيد كشف الحساب المالي (Account Ledger Balance)**: مصدر الحقيقة المطلق له هو مجموع وقيم حركات قيود اليومية المحاسبية المزدوجة المعتمدة في جدول خطوط القيود (`journal_entry_lines`)، وهو المرجع الأخير لإعداد القوائم المالية والأرباح والخسائر وحساب مديونيات الملاك والمستأجرين.

---

## T. Security & Granular Permissions (أمان وصلاحيات موديولات الـ ERP)

سنحافظ بالكامل على نظام الأدوار المعتمد والناجح حالياً (RBAC) مع إضافة الصلاحيات الدقيقة والتفصيلية التالية لربط موديولات الـ ERP الجديدة وحماية البيانات الحساسة:

### 1. الصلاحيات التفصيلية المستحدثة للـ ERP (Granular Permissions):
* `MANAGE_CHART_OF_ACCOUNTS`: صلاحية إعداد، تحديث، أو قفل شجرة الحسابات ودليل أستاذ الحسابات المحاسبي (خاصة بالـ SUPER_ADMIN و FINANCE).
* `MANAGE_INVOICES`: صلاحية إصدار الفواتير الضريبية، مراجعتها، أو إلغائها وتعديل قيم الضرائب المرفقة.
* `VIEW_OWNER_LEDGERS`: صلاحية الدخول على السجلات التحليلية وكشوف أستاذ الملاك ومطابقة العمولات والمصاريف.
* `APPROVE_OWNER_STATEMENTS`: صلاحية مراجعة وتأكيد واعتماد صرف كشوف مبالغ الملاك النهائية وتحويلها بنكياً.
* `RECORD_OPERATING_EXPENSES`: صلاحية تسجيل وإثبات المصاريف التشغيلية للمباني وتعيين من يتحمل تكلفتها.
* `RECORD_UNIT_INSPECTIONS`: صلاحية تحرير محاضر فحص الشقق والتقييم البصري للأضرار وتعيين الفنيين للإصلاح.

### 2. توزيع الصلاحيات والموديلات الجديدة على الأدوار الوظيفية الحالية:
* **SUPER_ADMIN**: يملك كافة الصلاحيات المذكورة أعلاه بشكل كامل ودون استثناء.
* **FINANCE (الإدارة المالية)**: يملك صلاحيات `MANAGE_INVOICES` و `VIEW_OWNER_LEDGERS` والقيود المحاسبية، ولكنه يملك صلاحية عرض وقراءة فقط لـ `chart_of_accounts` لمنع تعديلها دون موافقة الإدارة العليا.
* **PROPERTY_MANAGER**: يملك حصرياً صلاحية `RECORD_UNIT_INSPECTIONS` و `RECORD_OPERATING_EXPENSES` وتعيين الصيانة.
* **LEGAL (الشؤون القانونية)**: يملك صلاحية الاطلاع المحدود والربط المالي للقضايا الإيجارية والتسويات المبرمة.
* **TENANT (المستأجر)**: يملك صلاحية الاطلاع فقط على فواتيره الخاصة من خلال البوابة وطلب معاينة وفحص شقته.

---

## U. Future ERP Reporting Portfolio (حزمة التقارير المستهدفة للـ ERP)

يُصمم الـ ERP المستقبلي لتوفير منظومة تقارير تحليلية ومحاسبية شاملة تخدم متخذ القرار والإدارة المالية:

1. **تقرير ميزان المراجعة والأرباح والخسائر (Trial Balance / P&L)**: يعرض قائمة الدخل التفصيلية للشركة وصافي الأرباح التشغيلية، مستمد كلياً من جدول `journal_entry_lines`.
2. **تقرير ربحية وتدفقات العقارات والمباني (Property Profitability & ROI)**: يحسب صافي أرباح كل مبنى بشكل مستقل بمقارنة الإيرادات المحصلة من إيجارات وحداته مطروحاً منها مصاريفه التشغيلية وصيانته وعمولات الإدارة.
3. **تقرير العمولات المستحقة والمحصلة (Agency Commissions Report)**: يظهر عمولات الشركة الجارية، المحصلة، والمتأخرات المستحقة على الملاك والمستأجرين لكل عقد إيجار مبرم.
4. **تقرير الفواتير الضريبية وتتبع ضريبة القيمة المضافة (Tax & VAT Report)**: يوضح إجمالي مبالغ الضريبة المستلمة والمفروضة على الفواتير لتسهيل تقديم الإقرارات الضريبية الرسمية بدقة متناهية.
5. **تقرير محاضر الفحص وتقييم الوحدات السكنية**: يلخص نتائج تفتيش الوحدات ومستويات الأضرار المكتشفة ومبالغ الاقتطاع من مبالغ التأمين.

---

## V. Future ERP KPIs (المؤشرات القيادية الرئيسية للـ ERP)

تُحتسب المؤشرات التالية بشكل فوري وآني (Real-Time Metrics) في لوحة الإدارة لـ ERP لتوفير قراءة دقيقة للأداء التشغيلي والمالي:

* **معدل الإشغال الإجمالي (Occupancy Rate %)**:
  $$\text{Occupancy Rate} = \left( \frac{\text{Total Occupied Units}}{\text{Total Units available in Properties}} \right) \times 100$$
* **نسبة تحصيل الإيجارات الفعلية (Rent Collection Rate %)**:
  $$\text{Rent Collection Rate} = \left( \frac{\text{Total Collected Rental Income}}{\text{Total Invoiced Rental Installments}} \right) \times 100$$
* **نسبة ارتداد الشيكات البنكية (Cheque Bounce Rate %)**:
  $$\text{Cheque Bounce Rate} = \left( \frac{\text{Total Bounced Cheques Count}}{\text{Total Submitted Cheques Count}} \right) \times 100$$
* **صافي إيرادات مكتب الإدارة العقارية (Office Net Revenue)**:
  $$\text{Office Net Revenue} = \text{Collected Commissions} + \text{Collected Management Fees} - \text{Office Operating Expenses}$$
* **معدل ربحية العقار المستثمر (Property Return on Investment - ROI %)**:
  $$\text{Property ROI} = \left( \frac{\text{Collected Rental Rents} - \text{Operating Expenses}}{\text{Total Estimated Building Valuation}} \right) \times 100$$

---

## W. System Migration & Integration Plan (خطة الترحيل والترقية الآمنة)

### 1. خطة تهيئة وتوافقة هياكل البيانات (Schema Compatibilty & Backup):
* **تصدير لقطة بيانات كاملة وحفظها (Full Database Snapshot Export)**: قبل البدء بدمج الجداول الجديدة، يتم تفعيل ميزتي التصدير والنسخ الاحتياطي بالكامل لتوليد نسخة احتياطية مشفرة بصيغة JSON لكافة الكيانات الحالية.
* **البناء الإضافي وغير المدمر للبيانات (Additive Non-destructive DB Schema)**: يتم إنشاء الكيانات المحاسبية والمالية الجديدة وحقول التوسعة لعقود الإيجار والصيانة على شكل إضافات تدريجية في قاعدة البيانات السحابية والمحلية، بحيث لا يتأثر الكود الحالي بوجود هذه الحقول الفارغة في السجلات القديمة.

### 2. استراتيجية ترحيل ومطابقة البيانات المحلية (LocalStorage Migration & Sync):
* فور تحديث بنية الكود وطرح واجهات الـ ERP، يتم تفعيل مستشعر تلقائي في المتصفح يقوم بقراءة السجلات المخزنة محلياً بالهيكلية القديمة وتطبيق دالة ترحيل (Migration Function Wrapper) تقوم بإضافة الحقول الموسعة الجديدة بقيم افتراضية (مثلاً: `recordVersion: 1` و `syncStatus: "SYNCHRONIZED"`) لضمان عدم حدوث أي تعارض أو انهيار للواجهة عند التشغيل الأول.

### 3. سيناريو التراجع السريع والآمن عند حدوث خلل (Rollback & Recovery Protocol):
* في حال رصد أي مشكلات في توافق البيانات السحابية أو توقف واجهات المستخدم بعد طرح التحديثات:
  1. يتم التراجع فوراً برمجياً عن كود الواجهات الجديد واستعادة كود النسخة المستقرة السابقة (Stable Production Version) من مستودع التحكم في النسخ.
  2. سيعمل التوافق التنازلية المضافة لتظل البيانات القديمة والجديدة سليمة وتعمل الواجهات السابقة دون انقطاع لعدم تأثرها بالحقول الإضافية الفارغة.

---

## X. Implementation & Module Dependencies (تبعيات ومراحل التنفيذ)

لتنفيذ موديولات الـ ERP بكفاءة رياضية متكاملة، يجب الالتزام الصارم بترتيب مراحل التنفيذ التالية بناءً على التبعيات الفنية المتبادلة بين الأنظمة التشغيلية والمالية:

```
[مرحلة 1: التأسيس والعمود الفقري للـ ERP] ➔ إنشاء شجرة الحسابات والدليل المحاسبي (Chart of Accounts)
                 │
                 ▼
[مرحلة 2: نظام الفواتير والعمولات الموحد] ➔ إطلاق محرك الفواتير والعمولات وتتبع مستحقات المكتب والملاك
                 │
                 ▼
[مرحلة 3: كشوف الأستاذ والربط المالي الشامل] ➔ تفعيل كشف حساب المالك والمستأجر وربط المقبوضات المحاسبية المزدوجة
                 │
                 ▼
[مرحلة 4: إدارة النفقات والتشغيل والأصول] ➔ دمج المصاريف التشغيلية للمباني وإطلاق محاضر الفحص الفني للوحدات
```

---

## Y. Project Risks & Risk Mitigation Strategy (المخاطر والتحصين البرمجي)

* **خطر انخفاض سرعة التصفح ومعالجة البيانات (Performance Degradation)**:
  * *السبب*: كثرة عمليات الحساب الرياضية التراكمية في الواجهة لاحتساب الأرصدة والعمولات والمؤشرات المالية من كشوفات الحسابات المتعددة.
  * *التحصين البرمجي*: استخدام دوال الحفظ والذاكرة المؤقتة (React `useMemo` و `useCallback` Hooks) لتثبيت مخرجات الحسابات المالية وتجنب تكرار العمليات الرياضية المعقدة عند كل عملية تحديث بسيطة للواجهة، مع ترحيل عمليات التصفية المعقدة لتعمل في الخلفية.
* **خطر عدم توازن العمليات المالية في دليل الحسابات (Ledger Out-of-Balance)**:
  * *السبب*: حدوث انقطاع في الاتصال أو إغلاق المتصفح أثناء تسجيل دفعة مالية مما قد يؤدي لتسجيل سند القبض في جدول التحصيلات دون اكتمال تسجيل سطور القيد المالي المزدوج في دفتر الحسابات.
  * *التحصين البرمجي*: استخدام عمليات الكتابة المجمعة والصارمة في Firestore والذاكرة المحلية (**Firestore Batched Writes / Transactions**). يضمن هذا البروتوكول البرمجي إما نجاح تسجيل سند القبض وخطوط القيد المحاسبي المزدوجة تالياً ككتلة واحدة غير قابلة للتجزئة أو تراجع العملية كلياً وإلغاء التسجيل لمنع ظهور أي فروقات مالية مجهولة السبب.

---

## Z. Final Recommended ERP Architecture (التوصية المعمارية النهائية)

لضمان تحويل نظام **صقر الإمارات للعقارات** إلى ERP عقاري قياسي عالمي من الدرجة الأولى يحافظ على عملائك وبياناتك الحالية ويوفر أعلى درجات المرونة والتحكم المالي، نوصي برمجياً ومعمارياً بتبني المخطط البنيوي الهجين والتوافقي الموثق في هذه الدراسة الاستراتيجية الشاملة. 

البنية المصممة تضمن بقاء نواة النظام الحالي سليمة وتعمل بكامل كفاءتها، مع بناء طبقات الـ ERP المالية والمحاسبية والمحافظة العقارية والرقابية حولها كإضافات برمجية محصنة هندسياً ضد فقدان البيانات أو تعارض المزامنة.

---
*لقد تم إنجاز وصياغة مخطط هندسة بنية البيانات وتكامل الأنظمة لنظام الـ ERP العقاري الشامل بنجاح وبأعلى معايير الدقة المعمارية الاحترافية، وهو جاهز تماماً ليكون المرجع التقني المعتمد للتطوير البرمجي المستقبلي بناءً على توجيهاتكم الكريمة.*

**[تم الانتهاء بنجاح - في انتظار توجيهاتكم للمرحلة التالية]**
